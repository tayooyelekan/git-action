name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
            echo "test Hello World"
            echo "Hello 2 World"
      - name: Print goodbye
        run: echo "Done - bye"
